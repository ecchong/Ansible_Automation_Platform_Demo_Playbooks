---
- name: Direct query ServiceNow incident table
  hosts: all
  sources:
    - name: Watch for any updated table
      servicenow.itsm.records:
        table: incident
        interval: 5
        remote_servicenow_timezone: America/New_York
        timezone: America/New_York

  rules:
    - name: New incident
      condition: event.sys_id is defined
      actions:
        - print_event:
            pretty: true

