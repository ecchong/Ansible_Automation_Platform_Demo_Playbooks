---
sources:
  - ansible.eda.kafka:
      host: ryzen5.lab.automate.nyc:9094
      topic: syslog

rules:
  - name: Trigger on hello
    condition: event.message.message == "hello"
    action:
         
